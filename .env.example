# copy this file to .env (or just drop the .example from the name)
# docker compose picks it up automatically

# key used to sign login tokens, change it to something random for any public deployment
BOOKER_SECRET_KEY=change-me
